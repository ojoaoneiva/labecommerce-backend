export type Tuser = {
    id: string,
    name: string,
    email: string,
    password: string,
    creatAt: string
}

export type Tproducts = {
    id: string,
    name:string,
    price: number,
    description: string,
    imageUrl: string
}